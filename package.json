{
  "name": "gym-late",
  "version": "1.0.0",
  "description": "Track who arrives late to the gym — with group join codes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mysql2": "^3.11.5"
  },
  "optionalDependencies": {
    "better-sqlite3": "^9.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
